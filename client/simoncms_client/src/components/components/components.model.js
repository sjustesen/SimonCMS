export class ComponentModel {
    constructor(name, alias, status, description) {
        this.name = name;
        this.description = description;
        this.alias = alias;
        this.status = status;
    }
}